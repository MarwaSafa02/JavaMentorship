package OOPPack;

//(1)created the interface
public interface Movable3 {

    void move();
    //(2)
    // In an interface any method we declare has to be explicitly implemented by the class
    // that implements it because it is abstract, because abstraction is the purpose of interface.
    // ( Go back to Cart3) --> (3)
}
